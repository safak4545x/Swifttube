/*
 Overview / Genel Bakış
 EN: Right-side playlist panel for video pages; highlights current item, lazy-loads more, and jumps on selection.
 TR: Video sayfaları için sağdaki çalma listesi paneli; geçerli öğeyi vurgular, tembel yükler ve seçimle atlar.
*/

// EN: SwiftUI panel rendering playlist items and metadata. TR: Playlist ögelerini ve metadatasını çizen SwiftUI paneli.
import SwiftUI
